name: khuta
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ^3.8.0
dependencies:
  flutter:
    sdk: flutter  # Core
  cupertino_icons: 
  easy_localization:
  equatable: 
  google_fonts:
  flutter_bloc: 
  shared_preferences: 
  sqflite: 
  path:

  # Firebase
  firebase_core: 
  firebase_auth: 
  cloud_firestore: 

  # UI & Theming
  flutter_svg: 
  shimmer: 
  flutter_native_splash:

  # Utils
  uuid:
  intl: 

  flutter_tts:
  permission_handler: 
  connectivity_plus: ^6.1.4
  animated_bottom_navigation_bar: ^1.4.0
  animated_theme_switcher: ^2.0.10

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints:



flutter:
  uses-material-design: true

  # Add assets here
  assets:
    - assets/translations/
    - assets/images/
    - assets/images/teacher_questions/
    - assets/images/parents_questions/